# Calibration Data

Calibration data is a first-class project artifact.

Use `pump-trials-template.csv` for raw bench trials. Keep raw observations even
when they are ugly; bad trials often explain the actual machine better than the
polished chart.

